// TODO make this into a game with chips, a pot, blinds, bets, folds and multiple rounds (suggested order of difficulty)

fun main() {
    val playerSelection = PlayerSelection()
    StartGame().playTheGame(playerSelection)
}








